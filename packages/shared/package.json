{
  "name": "@claimreview/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TS types (mirrors the 14-field output contract) + API client for web & mobile",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
