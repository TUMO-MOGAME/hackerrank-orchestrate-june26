# Secrets
.env
code/.env

# Submission transcript log (lives in $HOME, but never commit if copied here)
log.txt

# Generated predictions (regenerated by main.py)
output.csv
code/output.csv

# Submission bundle (regenerable; uploaded to HackerRank separately, not tracked in git)
code.zip

# AI-generated benchmark fakes (large, regenerable via evaluation/gen_fakes.py)
dataset/images/_generated_fakes/

# Caches / artifacts
.cache/
code/.cache/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Virtualenvs
.venv/
venv/
env/

# Node / web / mobile
node_modules/
.next/
out/
.expo/
dist/
*.tsbuildinfo
apps/web/.env.local
apps/mobile/.env

# OS
.DS_Store
